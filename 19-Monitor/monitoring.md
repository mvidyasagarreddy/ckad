Monitoring is an essential aspect of managing a kubernetes cluster. It allows you to keep track of the health and performance of your applications and the cluster itself. Kubernetes provides several tools for monitoring, including:

kubectl top nodes

The `kubectl top` command is used to display resource usage of nodes and pods. It shows CPU and memory usage, which can help you identify resource bottlenecks and optimize your applications.

kubectl top pods
